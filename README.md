# Victor Discord Suite

A handler-driven Discord bot, API, and Next.js dashboard optimized for low-resource VPS deployments.

## Quick Start
```bash
pnpm install
pnpm -r build
pnpm -r dev
```

See `docs/` for deployment and configuration details.
